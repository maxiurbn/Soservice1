import { TipoContactoPipe } from './tipo-contacto.pipe';

describe('TipoContactoPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoContactoPipe();
    expect(pipe).toBeTruthy();
  });
});
